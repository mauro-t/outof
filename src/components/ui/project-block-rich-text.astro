---
import { toHTML } from "@portabletext/to-html";

const { blocks } = Astro.props;

const content = toHTML(blocks);
---

<div
  data-words
  style="font-kerning: none;"
  class="max-w-115 text-body-rem *:not-first:mt-5"
  set:html={content}
/>
